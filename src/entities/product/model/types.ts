export interface Product{
    id?: number,
    name?: string,
    description?: string,
    quantity?: number,
    price?: number,
    available?: boolean,
    productType?: string,
    ingredientsType?: string,
    burguerIngredients?: string,
}